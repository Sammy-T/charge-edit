{
  "name": "charge-edit",
  "version": "1.0.0",
  "description": "A basic text editor",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager ./ --all --ignore=^/documents --ignore=^/gh-img --out=./build --overwrite"
  },
  "keywords": [],
  "author": "Samuel Telusma",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.2.0",
    "electron-packager": "^15.1.0"
  },
  "dependencies": {
    "electron-default-menu": "^1.0.2"
  }
}
