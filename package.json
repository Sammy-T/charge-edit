{
  "name": "charge-edit",
  "version": "2.0.0",
  "description": "A basic text editor",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:local": "electron-packager ./ --all --ignore=^/documents --ignore=^/gh-img --out=./build --overwrite"
  },
  "keywords": [],
  "author": "Samuel Telusma",
  "license": "ISC",
  "devDependencies": {
    "electron": "^12.0.7",
    "electron-builder": "^22.11.1",
    "electron-packager": "^15.2.0"
  },
  "dependencies": {
    "electron-default-menu": "^1.0.2"
  }
}
